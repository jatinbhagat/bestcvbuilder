{
  "framework": "vite",
  "buildCommand": "rm -rf dist node_modules/.vite && npm ci && npm run build",
  "outputDirectory": "dist",
  "functions": {
    "api/**/*.py": {
      "runtime": "python3.9"
    }
  }
}