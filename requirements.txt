# Python API dependencies for BestCVBuilder - Vercel Serverless Compatible
# Only pure Python packages that work reliably in serverless environments

# Core dependencies
requests==2.31.0

# PDF/Word processing - Serverless compatible only
PyPDF2==3.0.1              # Pure Python PDF extraction
python-docx==1.1.0          # DOCX file support

# Database client
supabase==1.2.0

# Text processing utilities  
python-dateutil==2.8.2 