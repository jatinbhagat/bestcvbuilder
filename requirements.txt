# Python API dependencies for BestCVBuilder - Optimized for Vercel
# Core dependencies
requests==2.31.0

# PDF/Word processing - Critical for accurate text extraction
PyPDF2==3.0.1              # Basic PDF extraction (fallback)
python-docx==1.1.0          # DOCX file support
pdfplumber==0.10.3          # Enhanced PDF extraction (recommended)
pdfminer.six==20221105      # Comprehensive PDF extraction (best accuracy)
PyMuPDF==1.23.8             # High-performance PDF extraction

# Database (lightweight client)
supabase==2.0.2

# Text processing utilities
regex==2023.12.25
python-dateutil==2.8.2

# Data validation
pydantic==2.5.0 