# Python API dependencies for BestCVBuilder - Vercel Compatible
# Core dependencies
requests==2.31.0

# PDF/Word processing - Critical for accurate text extraction
PyPDF2==3.0.1              # Basic PDF extraction (fallback)
python-docx==1.1.0          # DOCX file support
pdfplumber==0.9.0           # Enhanced PDF extraction (Vercel compatible)
pdfminer.six==20221105      # Comprehensive PDF extraction (best accuracy)

# Database (lightweight client)
supabase==1.2.0             # Vercel compatible version

# Text processing utilities
python-dateutil==2.8.2

# Data validation (Vercel compatible version)
pydantic==1.10.12 